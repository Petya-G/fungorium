package view.game.vitem.spore;

public class VSplitterSpore extends VSpore {
    public VSplitterSpore(int x, int y, String toolTipText) {
        super(x, y, toolTipText);
        imagePath = "images/spore/splitter_spore.png";
        loadImage();
    }
}
