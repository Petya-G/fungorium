package view.game.vitem.spore;

import view.game.vitem.VItem;

public abstract class VSpore extends VItem {
    protected VSpore(int x, int y, String toolTipText) {
        super(x, y, toolTipText);
    }
}
