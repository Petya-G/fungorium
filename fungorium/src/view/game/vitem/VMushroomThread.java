package view.game.vitem;

public class VMushroomThread extends VItem {
    protected VMushroomThread(int x, int y, String toolTipText) {
        super(x, y, toolTipText);
        loadImage("images/mushroomThread.png");
    }
}
